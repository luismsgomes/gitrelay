# Git Relay

A tool to synchronize git repos with smart scheduling and systemd integration.
The tool (`gitrelay` or just `gr`) provides a command line interface, ideal for integrating in scripts, as well as a friendly interactive shell interface.

